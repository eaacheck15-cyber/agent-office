# AGENTS.md — Офис агентов (общий контекст для ВСЕХ агентов)

## Стандартные правила офиса

- Все агенты: автономия, доводить задачу до конца, возвращать фактический итог.
- Никогда не использовать абсолютный путь /output — только ./output внутри проекта.
- Визуальный анализ — через vision. Код — через venice. Клонирование — cloner.
- Пентест-задачи — через ts-*/pentai-*/loki-* (выбор: op-recommend).

## ⚙️ ПРАВИЛА ВЛАДЕЛЬЦА (жёсткие, для всех агентов и паука)

1. **Всё, что открыто и представляет ценность — КАЧАЕМ** (складываем в `./output` и БД loot.db).
2. **Открытое С ОШИБКАМИ = SQL-РАЗВЕДКА.** Если сайт отдаёт `sql syntax`, `SQL error`,
   `mysql`, `fatal error`, `stack trace`, `warning:` — это сигнал:
   - фиксируем в БД (open_data, type=error)
   - помечаем как кандидат на SQL-инъекционную разведку (полуактивную, через прокси)
3. Ценность строки определяется: открытый доступ + данные/ошибки/конфиг/креды.
4. Мусор (css/ico/статика/шаблонные заглушки) — НЕ качаем, в БД не пишем.

## 💰 ПРАВИЛО ЦЕННОСТИ (владелец, жёсткое — для всех агентов и паука)

1. **Ищем ТОЛЬКО деньги: то, что имеет реальную денежную/операционную ценность.**
   Всё остальное — мусор, в БД НЕ пишем.
2. **Шкала ценности каждой записи в loot.db:**
   - `$$$$` — открытая панель БД / `.env` с кредами / подтверждённая SQL-ошибка / доступ к данным
   - `$$$` — креды, API-ключи, JWT, токены, пароли серверов (MT5), рабочие email скамеров
   - `$$` — раскрытие: внутренние пути, версии софта с CVE, открытые API с данными, связки брендов
   - `$` — инфраструктура для карты: реальные IP, поддомены-админки
   - `0` — мусор: шаблоны (user@broker.com), парковки, CDN, ложные срабатывания regex, статика
3. **Запись в БД только если цена ≥ `$`.** `0`/мусор — не записываем, отбрасываем.
4. Паук/агенты обязаны ставить цену каждой находке перед записью в loot.db.

## 🔒 ПРАВИЛО ПРОКСИ (жёсткое, без исключений)
- ВСЕ сетевые операции — ТОЛЬКО через SOCKS-ротатор `socks5h://127.0.0.1:1080`
- Сверху — HTTP-прокси `http://4vZ956:E4RnNa@193.41.115.31:8000`
- ПРЯМЫЕ подключения ЗАПРЕЩЕНЫ. Никогда не ходить напрямую со своего IP.

## 🔬 ПРАВИЛА ХИРУРГИЧЕСКОЙ ПРОВЕРКИ (владелец)
1. Применяем инструменты аккуратно — НЕ наносим вред целостности, НЕ ломаем.
2. Смотрим всё, ничего не берём насильно.
3. НО если ресурс ОТКРЫТ (после пары манипуляций просто открыт) — ЗАБИРАЕМ.
4. Открытое = доступно без взлома/вреда. Закрытое — не ломаем.

## 🔒 РЕЖИМ ЗАКРЫТОЙ ПРОВЕРКИ (жёсткие правила, все агенты)
1. ВСЕ проверки — ТОЛЬКО через прокси-пул (SOCKS 127.0.0.1:1080 / мост 8081 / API 8912).
   Наши IP НИКОГДА не используются напрямую. Никаких прямых запросов.
2. Ничего не скачиваем насильно, целостность не нарушаем.
3. MySQL-агент: КОМПИЛИРУЕТ (анализирует/документирует), НО НЕ ЗАБИРАЕТ данные.
4. ЕСЛИ после манипуляций открылось (утечка произошла сама, ресурс открыт) — ЗАБИРАЕМ в облако (db-leaks).
5. Открытое = доступно без взлома/вреда. Закрытое — не ломаем, фиксируем.

## 💰 ИДЕИ НА ПРОДАЖУ (легальные, из собранного)
2. Досье открытых ошибок/уязвимостей (стек-трейсы, пути, версии) — баг-баунти/ответственное раскрытие
3. Инфраструктурная карта скамов (IP/хостинги/юрлица) — ИБ-компаниям
4. Подтверждённые открытые порты/БД — компаниям по кибербезопасности
5. Открытые утечки (когда открылись сами) — владельцам/ИБ

## 🔧 ПРАВИЛА КОВЫРЯНИЯ ДЫР (владелец, жёстко)
1. Нашли дыру → КОВЫРЯЕМ (полуактивные GET через прокси, не ломаем).
2. Всё, что НАРУЖУ (открытый доступ, без взлома) — НАШЕ, забираем.
3. Если в ходе разбора дыры всё видно (данные/инфо в открытом доступе) — ЗАБИРАЕМ.
4. Не входим внутрь закрытого, не эксплуатируем. Только то, что само открыто.
5. Собранное — в БД + облако, с оценкой ценности.

## 🎯 ПРАВИЛО GET/КАЧАТЬ (владелец, финально)
- GET-запросы АКТИВНЫ (проверяем открытость) — но НЕ качаем насильно.
- ЕСЛИ открыто (само отдаёт данные/ответ) — КАЧАЕМ.
- Закрыто/требует взлома — не трогаем, фиксируем.

## 🎯 МОДЕЛЬ «ОШИБКА → ДАННЫЕ НАРУЖУ» (владелец)
1. ИЩЕМ критичные ошибки: SQLi, дефолтные доступы, открытые API, раскрытие БД.
2. КОВЫРЯЕМ по ошибке ПОЛУАКТИВНО: смотрим, какие данные САМИ выходят наружу (без эксплуатации внутрь).
3. ЕСЛИ данные наружу И приносят доход (БД с пользователями, платёжные, балансы) — КАЧАЕМ в базу (loot.db).
4. Всё через паук (GET/POST через API валидных прокси), результаты в БД с оценкой.
5. Закрыто реально — не трогаем. Ошибка открыла — видим и качаем то, что само наружу.

## 🕷 ПРАВИЛО ПАУКА (владелец, финальное)
1. Паук НАХОДИТ ошибки → ШАРИТ (документирует) → КОВЫРЯЕТ (полуактивно).
2. ВИДИТ открытость: если наружу выходит ЦЕННОЕ (данные БД, пользователи, деньги) —
   это уже В СВОБОДНОМ ДОСТУПЕ → КАЧАЕМ в loot.db (с оценкой ценности).
3. Открытое = доступно без взлома (ошибка сама открыла). Качаем.
4. Закрытое = реально закрыто, не трогаем.
5. Всё через API валидных прокси, GET/POST полуактивные.

## 🔗 ЦЕПОЧКА АГЕНТОВ «ОТКРЫТОЕ С ДАННЫМИ» (владелец)
Задача: найти реально открытые системы с данными (пользователи/деньги) → качать в loot.db.
1. **ts-recon / pentai-recon-advisor** — разведка цели: поддомены, порты, тех-стек, панели.
2. **ts-web-attacker / pentai-api-security** — поиск открытых API/панелей без токена.
3. **pentai-database-attacker / database-administrator** — проверка БД: открытые порты 3306/1433,
   guest-доступ, дефолтные (пустые) учётки, БД без пароля.
4. **pentai-poc-validator** — подтверждение: вход реально открыт (без подбора/взлома).
5. **loki-loki / loki-vuln-scanner** — автономная проверка открытости через прокси.
6. **ts-report-writer** — документирование: карточка «что открыто, какие данные, оценка $».
ВСЁ через socks+proxy (ротатор 1080, мост 8081, API-пул 8912). Результат в loot.db.
